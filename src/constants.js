export const PALETTE = {
  // Harmonize with player sprite: deep base + soft green accent
  color1: '#1e2230',   // base/dark surfaces, matches CSS var(--color1)
  color2: '#76c893',   // soft green accent for shader pattern
  color3: '#eef3f6',   // light text/fill, cooler gray to match CSS
};

export const DIAGONAL_FACTOR = 1 / Math.sqrt(2);
export const ZOOM_MAX_BOUND = 2;
export const ZOOM_MIN_BOUND = 0.2;